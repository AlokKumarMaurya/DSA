name: firebase_service
description: "Package to store all firebase services at this directory"
version: 0.0.1
homepage:

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  firebase_core: ^3.2.0
  firebase_database: ^11.0.3
  cloud_firestore: ^5.1.0
  flutter:
    sdk: flutter
  firebase_messaging: ^15.0.3
  flutter_local_notifications: ^17.2.1+2
  local_storage:
    path: ../local_storage
  network_service:
    path: ../network_service
  firebase_auth: ^5.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:

