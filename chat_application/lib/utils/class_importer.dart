///This class is used to import the comman packages from the custom local packages so that we do not need to install the packages again and again in all packages
library;

export 'package:network_service/network_service.dart';
